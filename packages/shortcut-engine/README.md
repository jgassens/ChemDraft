# @chemdraft/shortcut-engine

Owns command-bound shortcuts, type-to-build behavior, conflict detection, and platform-aware modifier handling.

Do not hard-code important keyboard behavior only inside UI click handlers.
