# @chemdraft/style-compat

Compatibility boundary for external style-sheet inputs such as ChemDraw `.cds`.

This package converts supported style-sheet metadata into native ChemDraft style
presets. It does not parse molecules, reactions, pages, or documents.
