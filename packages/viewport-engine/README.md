# `@chemdraft/viewport-engine`

Small viewport math boundary for ChemDraft page scale, pan, ruler units, and coordinate conversion.

This package does not render UI. The desktop app uses it to keep zoom, ruler state, and screen/page conversion synchronized.
