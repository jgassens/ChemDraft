# @chemdraft/editor-shell

Owns editor-shell UI coordination around the drawing area.

This package should coordinate app chrome without owning the native document model or concrete editor internals.
