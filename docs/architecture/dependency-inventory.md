# Dependency Inventory

This file records dependencies added during the repository foundation and early implementation milestones.

**The project is licensed Apache-2.0** (finalized 2026-07-31). Every component below that requires
attribution is listed in the repository's `NOTICE`, which Apache-2.0 §4(d) obliges a redistributor to
carry. Adding a vendored binary means adding its row here *and* to `NOTICE` in the same change.

| Package | Purpose | License | Core or optional | Distribution impact |
| --- | --- | --- | --- | --- |
| `typescript` | Type checking for all workspace TypeScript code. | Apache-2.0 | Core development dependency | No runtime distribution impact. |
| `vitest` | Unit test runner for package skeleton tests. | MIT | Core development dependency | No runtime distribution impact. |
| `jsdom` | DOM implementation used by export-engine tests that exercise browser-oriented SVG/PDF conversion code under Node. | MIT | Core development dependency | Test-time only; no runtime distribution impact. |
| `@types/jsdom` | TypeScript declarations for jsdom-based export-engine tests. | MIT | Core development dependency | Type-checking only; no runtime distribution impact. |
| `vite` | Development server and production build for the desktop shell frontend. | MIT | Desktop app development dependency | Bundles frontend assets only. |
| `@vitejs/plugin-react` | React transform support for Vite. | MIT | Desktop app development dependency | Build-time only. |
| `react` | UI library for the desktop shell. | MIT | Desktop app runtime dependency | Included in the desktop frontend bundle. |
| `react-dom` | React DOM renderer for the desktop shell. | MIT | Desktop app runtime dependency | Included in the desktop frontend bundle. |
| `@jaames/iro` | Modular color picker widget used by ChemDraft color popovers for the custom color wheel/slider while ChemDraft keeps palette swatches and RGB/CMYK/HEX fields in owned React UI. | MPL-2.0 | Desktop app runtime dependency | Included in the desktop frontend bundle for color selection only. Pure JavaScript, no native build impact, no chemistry or document model impact. Direct runtime dependency licenses checked: `@irojs/iro-core` is MPL-2.0 and `preact` is MIT. Upstream is on maintainer hiatus, so keep the wrapper isolated in `ToolPalette.tsx`. |
| `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` | Sortable drag-and-drop for the Customize Toolbars editor's toolset and item lists (keyboard sensor included for accessibility). Used only inside `toolbars/CustomizeToolbars/`; toolbar state edits stay in the pure `layoutStateEdits` layer. | MIT | Desktop app runtime dependency | Included in the desktop frontend bundle for the customization dialog only. Pure JavaScript, React 19 compatible, no native build or document-model impact. |
| `@types/react` | TypeScript types for React. | MIT | Desktop app development dependency | No runtime distribution impact. |
| `@types/react-dom` | TypeScript types for React DOM. | MIT | Desktop app development dependency | No runtime distribution impact. |
| `zod` | Runtime schema validation for the native ChemDraft document model, plugin manifests/result envelopes, and toolset manifests. | MIT | Core runtime dependency | Included with code that validates native documents, plugin API inputs, and toolbar/toolset definitions. |
| `@scena/react-ruler` | React wrapper for Daybrush/Scena ruler rendering in the desktop document viewport. ChemDraft keeps viewport state and measurement math in `@chemdraft/viewport-engine`; this dependency only paints ruler canvases. | MIT | Desktop app runtime dependency | Included in the desktop frontend bundle for optional View > Show Rulers rendering. No chemistry or document model impact. |
| `@tauri-apps/cli` | Tauri v2 CLI for launching and building the desktop shell. | Apache-2.0 OR MIT | Desktop app development dependency | Required for desktop dev/build commands; no direct frontend runtime payload. |
| `@tauri-apps/api` | Official frontend API for invoking Tauri commands and listening for palette command events. | Apache-2.0 OR MIT | Desktop app runtime dependency | Included in the desktop frontend bundle for native window coordination only. |
| `ketcher-react` | React host component for the Ketcher molecule editor. Used only by the narrow active molecule editor host, not as the ChemDraft document/page model. | Apache-2.0 | Desktop app runtime dependency | Lazy-loaded into the desktop frontend bundle for selected molecule editing. Peer dependency warning observed through `miew-react` with React 19; keep under review. |
| `ketcher-standalone` | Standalone Indigo-backed structure service provider for Ketcher molecule editing without a remote service. | Apache-2.0 | Desktop app runtime dependency | Included for local Ketcher molecule editing. Brings Indigo/Ketcher runtime payload into the desktop frontend bundle; does not change `chem-core` document state ownership. |
| `raphael` | Explicit dependency used to satisfy Ketcher's browser-time dynamic Raphael request in the narrow desktop Ketcher host. | MIT | Desktop app runtime dependency | Included only for the selected-molecule Ketcher editor host; does not own ChemDraft document state or page rendering. |
| `fast-xml-parser` | Ordered XML parser for the CDXML-compatible `.chemdraft` envelope reader, visible-layer canonicalization, and progressive CDXML import support. | MIT | Core runtime dependency for `@chemdraft/cdx-compat` | Pure JavaScript; included with CDXML/CDX compatibility code. |
| `svg2pdf.js` | Converts ChemDraft's SVG page export into vector PDF drawing operations. Used only by `@chemdraft/export-engine` after SVG serialization has completed. | MIT | Core runtime dependency for `@chemdraft/export-engine` | Pure JavaScript; lazy-loaded by the desktop PDF export command. Direct transitive dependencies checked as MIT. No external stylesheet loading or network access is enabled. |
| `jspdf` | Creates PDF documents and byte output for the vector PDF export path. | MIT | Core runtime dependency for `@chemdraft/export-engine` | Pure JavaScript; lazy-loaded by the desktop PDF export command. Required transitives checked as MIT. Optional helper dependencies appear in the lockfile: `canvg`, `core-js`, and `html2canvas` are MIT; `dompurify` is `(MPL-2.0 OR Apache-2.0)` and is acceptable via Apache-2.0; `rgbcolor` ships an MIT license file despite package metadata also mentioning `FEEL-FREE.md`. ChemDraft does not call the optional HTML/canvas helper APIs in the initial PDF path. |
| `roughjs` | Deterministic sketch-effect path generation for native art graphics in `@chemdraft/art-engine`. | MIT | Core runtime dependency for native art effects | Pure JavaScript; used only to derive editable-object effect paths from native graphic geometry. Direct runtime dependency licenses checked as MIT: `hachure-fill`, `path-data-parser`, `points-on-curve`, and `points-on-path`. |
| `resvg` | Native SVG rasterization engine used by the desktop Tauri raster export command for PNG, JPEG, BMP, GIF, and TIFF. | Apache-2.0 OR MIT | Desktop app runtime dependency for native raster export | Included in the native desktop app binary. Added with `default-features = false` and only `text` plus `system-fonts`; the `raster-images` and `memmap-fonts` feature groups are not enabled. Resolved `resvg`/`image` dependency graph checked through Cargo metadata: licenses are permissive-only (`MIT`, `Apache-2.0`, `BSD-2-Clause`, `BSD-3-Clause`, `Zlib`, `Unlicense`, `0BSD`, and compatible OR/composite forms; `unicode-ident` includes `Unicode-3.0`). |
| `image` | Native image encoders for PNG, JPEG, BMP, GIF, and TIFF output after `resvg` renders the page SVG. | MIT OR Apache-2.0 | Desktop app runtime dependency for native raster export | Included in the native desktop app binary. Added with `default-features = false` and only the needed encoder features: `png`, `jpeg`, `bmp`, `gif`, and `tiff`. Resolved `resvg`/`image` dependency graph checked through Cargo metadata with no GPL/AGPL/copyleft packages found. |
| `@tauri-apps/plugin-dialog` | Native open/save/confirmation dialogs for desktop `.chemdraft` file IO. | Apache-2.0 OR MIT | Desktop app runtime dependency | Included in the desktop frontend bundle and native app plugin registration. |
| `@tauri-apps/plugin-fs` | Native text-file read/write for `.chemdraft` documents selected through the desktop dialog flow. | Apache-2.0 OR MIT | Desktop app runtime dependency | Included in the desktop frontend bundle and native app plugin registration with scoped file permissions. |
| `@tauri-apps/plugin-http` / `tauri-plugin-http` | The only outbound network path in the app: checking for and downloading trusted plugin releases (release metadata, the package `.zip`, and its `.sha256` sidecar). | Apache-2.0 OR MIT | Desktop app runtime dependency | Pinned to 2.5.9. Reachable **only** from the main window, through the separate `plugin-updates` capability rather than the default one, and its allowlist names the exact release URLs — every entry must be listed explicitly, because Tauri compiles them with `URLPattern` and matches the pathname exactly. Redirects are re-checked against a pinned GitHub asset host, responses are size-capped and time-limited, and downloads are SHA-256 verified against the sidecar before anything is staged. Adding a URL here widens the app's entire network surface; see `apps/desktop/src-tauri/capabilities/plugin-updates.json` and the guard test in `pluginUpdates.test.ts`. |
| `tauri` | Rust runtime for the native desktop shell. | Apache-2.0 OR MIT | Desktop app runtime dependency | Included in the native desktop app binary once Rust/Cargo is installed and the app is built. |
| `tauri-build` | Rust build helper for Tauri configuration/code generation. | Apache-2.0 OR MIT | Desktop app build dependency | Build-time only for the native desktop app. |
| `tauri-plugin-sparkle-updater` + Sparkle 2 | Bridges the packaged Tauri app to Sparkle's native macOS updater UI, scheduled checks, and Ed25519-verified app replacement. | MIT | macOS desktop runtime dependency | The Rust bridge is pinned to 0.2.4. Sparkle.framework is pinned to 2.9.4, downloaded from the official release, and SHA-256 verified before local extraction. Updates replace only `ChemDraft.app`; installed plugins remain in the separate Application Support data directory. |
| `objc2-app-kit` | Narrow macOS AppKit bridge for utility-palette window behavior that Tauri does not expose directly. | Zlib OR Apache-2.0 OR MIT | Desktop app runtime dependency | macOS-only native binary dependency for floating toolset window level, deactivation hiding, and utility-window behavior. |
| `serde` | Rust serialization derives for narrow Tauri command/event payloads. | Apache-2.0 OR MIT | Desktop app runtime dependency | Included in the native desktop app binary through Tauri command payload types. |
| `serde_json` | Serializes/reads the native palette persistence files (window geometry in `toolbar-state.json`, and the JS-owned layout state in `toolbar-layout-state.json`). Rust no longer parses the toolset manifest — TypeScript owns toolbar structure and pushes the menu/window model. | Apache-2.0 OR MIT | Desktop app runtime dependency | Included in the native desktop app binary for palette-geometry and layout-state persistence. |

## Chemistry engines, vendored binaries, and bundled data

Added 2026-07-30 on `chemdraft-analyzers`. These were absent from the table above, which recorded every
JavaScript UI package down to its transitives but no chemistry engine. Rows here carry the fields the
property/prediction suite plan requires: exact version, hash, source URL, license, notice file, local
modifications, build flags, and bundled data. Facts only — the core license choice and the redistribution
decisions remain the project owner's (PLANS.md §4).

| Component | Purpose | License | Core or optional | Distribution impact |
| --- | --- | --- | --- | --- |
| RDKit MinimalLib WASM (vendored) | The chemistry engine: ETKDGv3 3D embedding and force-field refinement for Spin 3D today, and the descriptor/identifier/substructure engine for the property suite. Committed artifacts, not an npm dependency. | BSD-3-Clause | Core runtime dependency for `@chemdraft/rdkit-adapter` | Vendored at `packages/rdkit-adapter/vendor/`. Source `https://github.com/rdkit/rdkit` tag `Release_2026_03_3`, commit `e74e7b0a5a2fc4e7f77c04ec26a61d4b8edbf22f`. Built 2026-07-30 via the RDKit Docker MinimalLib flow with `EXCEPTION_HANDLING=-fwasm-exceptions`. `RDKit_minimal.js` 102 KB SHA-256 `90d756278675169aaa8753b5475b61c2d3efa1efa6fb9c3aa83bec481687f0cf` (unchanged from the 2026-06-15 build — embind registers from the wasm, so added C++ methods do not alter the Emscripten glue); `RDKit_minimal.wasm` 7.5 MB SHA-256 `48b725a2e80af7f9792cd56abf65f16cea402f1cfe4f3f6c32a71669f1d848aa`. Notice file `vendor/RDKit-LICENSE.txt`. **Local modifications: 6 patches** (`vendor/patches/0001`–`0006`) linking the conformer/force-field libraries into the Embind target, declaring/implementing/registering `generate_3d_embed` and `optimize_3d_conformer`, redirecting the deps image's FreeType download to a working mirror, and exposing RDKit's `includeSandP` on `get_descriptors` so TPSA can select the sulfur/phosphorus convention; recipe in `vendor/BUILD.md`. **Keep this count current; the budget is tracked deliberately** (PLANS.md §7 — a tenth patch is a maintenance smell). **All six patches are in the committed binary as of 2026-07-30**, verified live rather than assumed; the runtime still detects the `includeSandP` capability by value, because the pre-rebuild binary ignored the argument silently instead of rejecting it and any future artifact lagging the patch set would too. `PINNED_RDKIT_WASM_SHA256` in `src/methods.ts`, the hash in `vendor/BUILD.md`, and the bytes on disk are checked against each other by `methods.test.ts`. |
| IsoSpec WASM (vendored) | The isotope-envelope engine: per-isotope abundances and the exact convolution behind `DistributionResult`. Committed artifacts, not an npm dependency. | BSD-2-Clause | Core runtime dependency for `@chemdraft/isospec-adapter` | Vendored at `packages/isospec-adapter/vendor/`. Source `https://github.com/MatteoLacki/IsoSpec` tag `v2.3.5`, commit `e6b1ef7cc146632cdaaf887dcff8c73949167835` — **pinned by commit because the tag and the in-tree `CMakeLists.txt` version (2.3.4) disagree**. Built 2026-07-30 with emsdk 6.0.0 and `-fwasm-exceptions`, reusing the RDKit deps image purely as a toolchain; **rebuilt 2026-07-31 from the same commit** to add the explicit-isotope entry point, which moved the artifact hashes without changing IsoSpec itself. `IsoSpec.js` 33 KB SHA-256 `d7cf0bf7018221080c12864bab3abc46020f14386a13168bf7b99a83d988342a`; `IsoSpec.wasm` 234 KB SHA-256 `2199faaddd03df4c6d9bca79a2b1ec673608e90b8ba58940bf67d9e6b79a954a`. Notice file `vendor/IsoSpec-LICENCE.txt`; © 2015–2025 Michał Startek and Mateusz Łącki. Binary redistribution must reproduce the copyright notice, conditions, and disclaimer in the accompanying materials. **Local modifications: none** — IsoSpec is compiled unpatched and reached through our own Embind wrapper (`vendor/src/chemdraft_isospec.cpp`), which adds no chemistry. Recipe in `vendor/BUILD.md`. The abundance data it carries has no upstream provenance; see the isotope-abundance section below. |
| OPSIN (vendored JAR + bundled JRE) | The name→structure engine: systematic chemical name to SMILES. Committed artifact plus a `jlink`ed Java runtime, not an npm dependency. | MIT (OPSIN); the shaded jar also redistributes Apache-2.0 and BSD-2-Clause code, and the bundled runtime is GPLv2+CE | Optional — backs the `opsin-name-to-structure` plugin | Vendored at `apps/desktop/src-tauri/resources/opsin/`. Source `https://github.com/dan2097/opsin` release `2.9.0` (2026-03-15). `opsin-cli-2.9.0-jar-with-dependencies.jar` 13.7 MB SHA-256 `c2e29326c281f87b59a05d934d8589adac6e9d17b95b984931b3e739111b360f`. **Must be the `cli` jar** — 2.9.0 removed `main` from `NameToStructure`, so the 4.9 MB `core` jar has no `Main-Class` and cannot run. **Local modifications: none** — shipped byte-for-byte; repackaging to strip `log4j-core` would save ~20 MB but makes the hash unverifiable against the upstream release. **Shading clobbers `META-INF`**, so the jar's own LICENSE/NOTICE files are incomplete and this repo must carry the notices: bundled and actually shipping code are opsin-core 2.9.0 (MIT), woodstox-core 7.1.1 (Apache-2.0), log4j-api **and log4j-core** 2.25.3 (Apache-2.0), commons-io 2.21.0 (Apache-2.0), stax2-api 4.2.2 (BSD-2-Clause), and dk.brics automaton 1.12-4 (BSD-2-Clause) — licences resolved from each Maven Central POM, walking to the parent where the child declares none. `net.java.dev.msv:xsdlib` and `com.sun.xml.bind.jaxb:isorelax` appear in `META-INF/maven` but ship **zero classes**, so they are not redistributed. **No copyleft in the jar.** The runtime is a 47 MB `jlink` image (module list and recipe in `resources/opsin/BUILD.md`); it is bundled because macOS ships no JRE — `/usr/bin/java` is a stub — so detect-only would leave the feature off for most users. A redistributed OpenJDK runtime is GPLv2+CE and needs its own notice. Notice file `OPSIN-LICENSE.txt`. |
| InChI (inside the RDKit MinimalLib WASM) | Standard InChI and InChIKey generation, reached through `mol.get_inchi()` and `RDKit.get_inchikey_for_inchi()`. Not separately vendored — statically linked into the artifact above. | **To confirm** | Core runtime dependency, transitive through RDKit | The binary self-identifies as `InChI version 1, Software 1.07.3 (API Library)` (read from `RDKit_minimal.wasm`). The InChI Trust's terms changed across the 1.06→1.07 series, so the applicable license and notice obligations for 1.07.3 must be confirmed against the InChI Trust's published terms and the notice archived before any redistribution. Recorded here with the exact version so the distribution track has something specific to clear. |
| FreeType (build image; linkage to confirm) | MolDraw2D SVG font rendering inside RDKit. Unrelated to 3D embedding or descriptors; present because the RDKit deps image builds it unconditionally (see patch `0005`). | FTL OR GPL-2.0 (dual; the choice must be recorded) | Optional, transitive through RDKit | Whether FreeType is actually linked into the shipped `RDKit_minimal.wasm` — as opposed to only built in the deps image — must be verified before release, and the license election recorded if it is. A GPL-2.0 election would be incompatible with the core graph; the FTL election is the one to take. |
| pKa model artifacts (vendored, trained here) | The trained random forest and its calibration that produce every pKa the app reports. Six committed JSON artifacts, generated by `packages/rdkit-adapter/vendor/pka-model/run_all.sh`, not an npm dependency. | Apache-2.0 (this repo's own trained weights); the TRAINING DATA is under two separate upstream licences, below | Core runtime dependency for `@chemdraft/rdkit-adapter` | Vendored at `packages/rdkit-adapter/vendor/pka-model/`. `site-pka-forest.json` 3.6 MB, plus `calibration.json`, `consensus-calibration.json`, `coupling.json`, `hammett-sigma.json` and `external-validation.json`; per-file hashes and the manifest are in that directory's `BUILD.md`. `PINNED_PKA_MODEL_SHA256` in `src/methods.ts`, the prose in that `BUILD.md`, and the bytes on disk are checked against each other by `methods.test.ts`, and the manifest travels in every ionization-bearing run's engine environment. **Training data is not vendored** — the corpus is rebuilt from two upstream sources: 3,031 labels from the **Dwar-iBond** set as distributed by Uni-pKa (`https://github.com/dptech-corp/Uni-pKa`, Apache-2.0), and 4,022 from the **QupKake** experimental set (`https://github.com/Shualdon/QupKake`, BSD-3-Clause, originally Baltruschat & Czodrowski). QupKake's VALUES are measurements but its SITE INDEX is ChemAxon Marvin's assignment, recorded per row rather than presented as measured. **Open question:** the DataWarrior author has stated the original literature references for part of the Dwar-iBond set were lost, so those rows cannot all be audited to a measurement, and a software licence does not automatically license an associated dataset — this must be resolved before public redistribution. |
| `openchemlib` 9.22.1 | 3D conformer generation (`ConformerGenerator` + `ForceFieldMMFF94`) behind the spin tool, and the comparison engine for logP/logS/tPSA in the expert view. Lazy-loaded (~2.3 MB) so it never touches app startup. | BSD-3-Clause | Core runtime dependency for `@chemdraft/ocl-adapter` | Pure JavaScript from npm; notice file `LICENSE` in the package. Bundled data: OCL's static torsion resources, registered once via `Resources.register*`. **No pKa predictor** — `pKaPredictor`/`pKaPlotter` are DataWarrior-side, not in this package. |
| `avogadrolibs` (Avogadro Core + Calc) | The interactive-3D sidecar's UFF optimizer. Built from source into `avogadro3d-sidecar`, not linked into the app binary. | BSD-3-Clause | Optional native sidecar dependency | Pinned to `OpenChemistry/avogadrolibs` commit `416651ddaef33a4e20392392e7c0b505d446491b`. Built with `CHEMDRAFT_AVOGADRO3D_ENABLE_OPENBABEL=OFF`, `CHEMDRAFT_AVOGADRO3D_ENABLE_GPL_PLUGINS=OFF`, and `CHEMDRAFT_AVOGADRO3D_ENABLE_DIAGNOSTIC_VIEWPORT=OFF`; `pnpm audit:engine3d-sidecar` fails the build if those guards or the pinned commit change. Only `avogadro3d-sidecar-aarch64-apple-darwin` is a real binary (1.1 MB); the Intel-macOS, Linux, and Windows `externalBin` entries are 113–141-byte placeholders (`apps/desktop/src-tauri/binaries/README.md`). |
| Eigen 3.4.0 | Header-only linear algebra required by avogadrolibs. Build-time only. | MPL-2.0 (with legacy LGPL-licensed modules that this build must be confirmed not to use) | Optional native sidecar build dependency | Cloned at `--branch 3.4.0` from `https://gitlab.com/libeigen/eigen` into `native/avogadro3d-sidecar/_deps/eigen`. Header-only: no separate runtime artifact ships, but the MPL-2.0 notice obligation travels with the sidecar binary. |
| nmrshiftdb2-derived reference database | Statistics behind the shipped ¹H/¹³C NMR predictor, distributed with the standalone NMR plugin rather than the app. | nmrshiftdb2 Database License (ODbL-derived) — **share-alike and attribution obligations travel with any redistribution** | First-party plugin data, not core | Not in this repository's tree; lives with `org.chemdraft.nmr.predictor`. The plugin's original code is MIT (finalized 2026-07-16); the database is **not** covered by that MIT grant and must never be described as MIT (AGENTS.md §8a). The open interpretation question — whether an MIT plugin running only inside an unlicensed host satisfies the license's functional-end-user-software clause — is tracked in PLANS.md §4 and is the highest-priority distribution-track item. |

## Copyleft scan (Release 1 closeout, 2026-07-30)

`pnpm licenses list` over the JS graph and `cargo tree --format "{l}"` over the Rust graph, run as the
PLANS.md §10 closeout check ("confirm no GPL/AGPL package enters the core graph").

**No GPL or AGPL anywhere in either graph.** The Rust graph is permissive-only. Three JS entries needed
resolving by hand rather than accepting the tool's answer:

| Package | Reported | Resolved | Disposition |
| --- | --- | --- | --- |
| `rollup-plugin-dts` 6.4.1 | `LGPL-3.0-only` | as reported | The only copyleft in the tree. A **root devDependency**, used solely by `scripts/build-sdk.mjs` to bundle plugin-SDK `.d.ts` files. Build-time tool: it is not linked, not bundled, and not redistributed, and its output is type declarations rather than a derivative of the plugin. Recorded because "no copyleft" would otherwise be an overstatement. Revisit if the SDK build ever ships the tool itself. |
| `eve-raphael` 0.5.0 | `Unknown` | **Apache-2.0** (from the package's own `LICENSE`) | Transitive of `raphael`, itself present only for the Ketcher host's browser-time dynamic require. "Unknown" is pnpm reporting a missing `license` field in `package.json`, not an absent licence. |
| `font-face-observer` 1.0.0 | `BSD` | **BSD-2-Clause** (two condition clauses, no non-endorsement clause) | Transitive of `ketcher-react`. The bare `"BSD"` string predates SPDX identifiers. |

Everything else resolves to MIT, Apache-2.0, BSD-2/3-Clause, ISC, 0BSD, Zlib, Unlicense, CC0-1.0,
BlueOak-1.0.0, MPL-2.0 (`@jaames/iro` and `@irojs/iro-core`, already recorded above), or a permissive
`OR` composite.

## Isotope-abundance data — the Release 2 gap, closed 2026-07-30

**Resolved by vendoring IsoSpec** (see the row above). The gap analysis below stands as the record of
why a new dependency was necessary rather than a table lookup on something already present.

Release 2's isotope envelope needs per-isotope natural abundances. **Nothing already in the dependency
graph supplies them**, checked directly rather than assumed:

- The vendored RDKit MinimalLib exports no isotope or abundance accessor — `get_descriptors` gives
  `amw` and `exactmw` for a whole molecule and nothing per-isotope.
- OpenChemLib 9.22.1 exposes no abundance table and no isotope-pattern calculator. Its public surface
  has `getMolecularFormula()` (absolute and relative weights, i.e. the same two numbers RDKit gives),
  `getAtomMass`/`setAtomMass`, and `isNaturalAbundance` — no distribution engine.

So the envelope needs one of: **IsoSpec** (BSD-2-Clause, brings both the algorithm and its data) built
through the Emscripten lane, or a separately licensed abundance table. PLANS.md §6 is explicit that
NIST's Standard Reference Data can carry terms, so a table cannot simply be copied in — its title,
version, endpoint, and terms have to be recorded first.

**IsoSpec was chosen, and it avoids that problem rather than solving it.** Its abundances arrive under
BSD-2-Clause as part of the library, so no separate data licence is engaged. What it does *not* bring is
provenance: IsoSpec's element tables carry no citation and no generator in its own repository. The
honest statement is therefore "these are the values the shipped engine used", which is why
`isotope_table()` reads them back out of the binary. Measured against the artifact: 292 entries, every
element normalised to sum to 1, masses matching published AME values to ~1e-9, and abundances that
differ from the commonly quoted CIAAW representative values — ¹³C is `0.010788` against CIAAW's
`0.0107`, 0.82% relatively higher. CIAAW publishes carbon as an interval (0.9884–0.9904) because it
varies by source, so this is a convention to disclose in the method contract, not a discrepancy to fix.

**A second unsourced table used to ship, and no longer does.** `examples/plugins/mass-fragment-demo`
carried an eight-element abundance table (`ISOTOPE_ABUNDANCES` in `src/massAnalysis.ts`) with no source
or terms recorded, feeding a first-order M/M+1/M+2 estimate. Both were **removed on 2026-07-30**: the
plugin now asks the host for the real envelope through the `chemistry.compute` capability, so there is
one abundance table in the repository rather than two, and it is the one whose provenance is documented
above. The plugin is separately MIT-licensed; consolidating its adduct table with the core's remains a
product decision, not a compliance one.
