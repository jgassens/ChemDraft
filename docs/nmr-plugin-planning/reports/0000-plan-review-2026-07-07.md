# Report 0000: Plan review and workspace bring-up (2026-07-07)

First-session review of `PLANS.md`, `AGENTS.md`, and `FIRSTPROMPT.md`,
verified against the live ChemDraft repository
(`~/Documents/programming/chemdraw`, `main` @ `64cf513e`, clean).

## Verdict

The three documents were architecturally sound — correct instincts on
fixture-first prediction, canary-first runtime bring-up, ownership
boundaries, worker discipline, and scientific-claim honesty — but they were
written against a stale snapshot of the repository. The repo's last commit
("Add plugin toolset contributions, selection.read, storage, and panel
APIs") had already shipped two things the plan proposed to invent (a
selection API; panel plumbing) with **different designs** than the plan
specified. Unrevised, the first agent run would have either built duplicate
parallel APIs or "corrected" deliberate repo decisions.

## What was verified

Full ledger with evidence: `STATUS.md` → "Assumption ledger" (19 rows).
Highlights:

- **Confirmed:** no desktop `PluginHost` instantiation; `CommandRegistry`
  built in a `useMemo` in `MainWindow.tsx` (~23,500 lines); `invokeCommandRef`
  and conformer worker/client patterns; no analysis store; no plugin
  diagnostics UI; permissions list incl. `analysis.write`; structure-format
  enum incl. `unknown`; OpenChemLib `^9.22.1` already shipped.
- **Stale/wrong in the plan:** a permission-gated optional
  `PluginSelectionAPI.getSelection()` already exists with tests; a
  declarative `PluginPanelReport` model (text/keyValue/table/svg, svg
  ≤ 512 KB, script-inert) already exists with host routing — the plan's React
  panel-component registry contradicted it; an Analyze menu already exists,
  mirrored in the native Tauri menu by a drift test with an exclusion
  precedent; IDs follow `plugin.<name>.*` naming the desktop keys on;
  manifest examples use `apiVersion: "^0.1.0"`.
- **Newly discovered constraints:** per-branch AGENTS.md convention with a
  build stamp; pending selection-policy refactor (`PLANS-selection-policy.md`);
  five example plugins (mass-fragment-demo is the natural second analyzer);
  notary instructions at `~/Documents/programming/.notary`.

## Changes made

**PLANS.md** — added "Repository verification (2026-07-07)" section;
corrected the current-state section; rewrote "Bundled panel registry" →
"Panel reports and the desktop report renderer" (ADR-0004); resolved the
menu Design A/B question via the appMenu model + drift-test exclusion
(ADR-0009); added "Reconciliation with the existing selection API" and
recast M4 as an extension (ADR-0008); fixed the manifest example (IDs,
`^0.1.0`, full 12-key contributions shape, trusted-validation note);
rewrote the command-flow example against the real context APIs; recast
Phase 1 UI as report sections + desktop chrome; replaced the plugin `ui/`
React folder with a pure `report/` module; specified `stableHash` (FNV-1a,
synchronous, non-cryptographic); added worker-client ownership and a
cross-package worker-bundling risk note; added a generic staleness mechanism
(optional `source` ref on reports + desktop chrome banner); added an
injectable ID factory + unbounded-store note; added the **Extension-point
inventory**; added extensibility acceptance tests 91–93; marked the
paste-ready Codex task **superseded**.

**AGENTS.md** — adopted repo conventions (build-stamp header, worktree/
branch block, notary pointer); execution order now mirrors canonical M1–M12;
reading list updated (appMenu + drift test, toolsets.ts, selection-policy
plan, targeted MainWindow regions); declarative-panel rule replaces the
panel-registry rule; selection rules updated to extend-in-place; new
"Naming and manifest conventions" section; report-archiving instruction.

**New system** (extensible to future plugins): `STATUS.md` (milestones +
assumption ledger + open decisions + risks), `prompts/` (README, TEMPLATE,
revised assignment 01, draft assignment 02), `decisions/` (ADR-0001…0009),
`reports/` (this file), workspace `README.md`. `FIRSTPROMPT.md` is now a
pointer to `prompts/01-runtime-bringup.md`.

## Open items

- D-01/ADR-0009 (menu drift-test exclusion) and D-03 (panel interactivity)
  need validation during assignment 01 / design in the M9 prompt.
- D-04: worktree/branch name — pending user (suggested `chemdraw-nmr`,
  `codex/nmr-plugin`).
- Worker bundling across workspace packages — retire with a spike early in
  M7.
- This workspace is not a git repository; recommend `git init` + initial
  commit so plan evolution is diffable.

## Next step

Cut the NMR worktree, drop in `AGENTS.md` + `PLANS.md`, fill in the
worktree/branch header, and issue `prompts/01-runtime-bringup.md`.
